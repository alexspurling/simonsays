Todo

+ Add click handlers to each element of the SVG (no idea how?)
+ Generate a random sequence of colours for the initial sequence
+ Use random sequence to display highlights
+ Shorten the delay between highlights
+ Use an index to track which is the expected colour to wait for
+ Verify the correct colour was chosen
+ Create two Msg types for Logo and Main and use Html.App.map to map one to the other
+ start with a short sequence
+ if player guesses correctly, add to sequence
+ if player guesses incorrectly, start new game
+ Play sounds in an audio buffer

- Create call back port to receive loaded sounds
- Build with gulp
- Use sound sprite sheet
